sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t gourab/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true gourab/docker-react npm run test
